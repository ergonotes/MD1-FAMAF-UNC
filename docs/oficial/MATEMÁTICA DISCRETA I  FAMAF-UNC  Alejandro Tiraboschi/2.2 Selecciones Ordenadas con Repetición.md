A lo largo de las notas se recurre a los conceptos de Ordenado y Sin repetición:

- **Ordenadas:** El orden en que eliges los elementos importa (por ejemplo, elegir "A y luego B" es diferente de "B y luego A").
- **Sin repetición:** No puedes elegir el mismo elemento más de una vez.
****
Formalmente, si $A,B$ conjuntos y definimos el producto cartesiano entre $A$ y $B$ por $A×B={(a,b) : a ∈ A,b ∈B}$. Entonces si $A$ y $B$ son conjuntos finitos se cumple que $|A×B| = |A|·|B|$.

Un aplicación inmediata del principio de multiplicación es que nos permite calcular la cantidad de selecciones ordenadas con repetición.

**Ejemplo.** Sea $X = {1,2,3}$ *¿de cuántas formas se pueden elegir dos de estos números en forma ordenada?*
Son $9 =3^2$ formas. Es claro que para la primera elección tenemos $3$ valores posibles y para la segunda elección tenemos también $3$ valores posibles, entonces, por el principio de multiplicación, tenemos en total $3·3$ elecciones posibles.
****
**Ejemplo**. Sea $X$ un conjunto de $m$ elementos. Queremos contar cuántos subconjuntos tiene este conjunto. Por ejemplo, si $X = {a,b,c}$ los subconjuntos de $X$ son $∅, {(a)}, {(b)}, {(c)}, {(a, b)}, {(a,c)},{(b,c)},{(a,b,c)}$. Es decir que existen $8$ subconjuntos de $X$, un conjunto de $3$ elementos. 
Una forma de verlo seria la siguiente: podemos identificar cada subconjunto de $X$ con una terna ordenada de $0’$s y $1’$s de la siguiente manera: si $a$ está en el subconjunto la primera coordenada de la terna es $1$, si no es $0$; si $b$ está en el subconjunto la segunda coordenada de la terna es $1$, si no es $0$; y análogamente con $c$. Tendríamos la identificación:
![[Pasted image 20250324212533.png]]
Observar entonces que seleccionar un subconjunto de $X$ es equivalente a elegir en forma ordenada $3$ elementos del conjunto {$0,1$}; y sabemos entonces que en ese caso tenemos $23$ posibilidades. 

En general , cuando $X$ tiene $n$ elementos, elegir un subconjunto de $X$ es equivalente a elegir en forma ordenada $n$ elementos del conjunto {$0,1$} y por lo tanto:

**Proposición 2.2.2.** La cantidad de subconjuntos de un conjunto de $n$ elementos es $2^n$.

Dado $X$ un conjunto, denotamos $P(X)$, partes de $X$, al conjunto formado por todos los subconjuntos de $X$, por ejemplo $P({1,2})={∅,{1},{2},{(1,2)}}$.

Si $X$ es un conjunto finito: $|P(X)|=2^{|X|}$
****
--8<-- "includes/algebra2.md"
